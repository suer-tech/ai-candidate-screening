## 1. Независимый приёмочный baseline

- [x] 1.1 Независимому субагенту, не участвующему в реализации change, создать исполняемый приёмочный тест по implementation slice `VAC-017` и `VAC-019`: отсутствие направлений, пустое название, пустые A/B/C, нормализованные дубликаты, сохранение черновика после отказа и одно валидное сохранение.
- [x] 1.2 Запустить новый тест на текущей реализации, зафиксировать ожидаемый RED и доказательства и подтвердить, что падение вызвано отсутствующим поведением, а не неисправностью тестовой среды.

## 2. Модуль ABC-валидации

- [x] 2.1 Добавить чистый типизированный валидатор ABC-профиля, возвращающий collection-, direction- и field-level ошибки без изменения значений и порядка входных направлений.
- [x] 2.2 Реализовать проверки наличия хотя бы одного направления, непустых после trim названия и A/B/C и регистронезависимой уникальности названий после удаления окружающих пробелов.
- [x] 2.3 Добавить focused unit tests для каждого правила, структурированной локализации ошибки, пограничных случаев нормализации, валидного результата и неизменности входных данных.

## 3. Интеграция редактора вакансии

- [x] 3.1 Подключить валидатор на save boundary `VacancySettings`, чтобы невалидный черновик не изменял отображаемую версию и не запускал уведомление об успешном сохранении.
- [x] 3.2 Вывести постоянные collection- и field-level ошибки и связать их с соответствующими input и textarea через доступные invalid-state атрибуты, сохранив краткое уведомление о причине отказа.
- [x] 3.3 Очищать либо пересчитывать устаревшую ошибку при изменении соответствующего поля, не удаляя, не нормализуя, не переупорядочивая и не пересоздавая остальные данные черновика.
- [x] 3.4 Сохранить существующий valid-save flow и проверить, что одна принятая попытка увеличивает отображаемую версию ровно на единицу и сообщает HR новый номер.

## 4. Проверка и release gate

- [x] 4.1 Повторно запустить независимо созданный acceptance test и focused validator/UI tests и зафиксировать GREEN для каждого сценария текущего implementation slice.
- [x] 4.2 Запустить `npm test` и `npm run lint` из `web/`, устранить внесённые change регрессии и отдельно отметить ранее существовавшую lint-ошибку, если она не относится к реализации.
- [ ] 4.3 Запустить обязательный набор `E2E-VAC-001`, `E2E-TRN-001`, `E2E-ABC-001` и `E2E-RESULT-001` на одной сборке и конфигурации. Если тест отсутствует, пропущен или не имеет доказательств, оставить change незавершённым и зафиксировать production-gate blocker.
- [x] 4.4 Проверить, что evidence package использует синтетические данные, не содержит секретов или персональных данных реальных кандидатов, идентифицирует сборку и конфигурацию и фиксирует независимых автора и исполнителя приёмочного теста.
