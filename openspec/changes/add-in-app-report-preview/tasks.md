## 1. Независимый приёмочный baseline

- [x] 1.1 Независимому субагенту, не участвующему в реализации change, добавить исполняемые сценарии TST-077/TST-078 для скрытия прежней версии при повторе, новой актуальной пары, соответствующих modal preview, стандартных read-only viewer actions, возврата карточки в состояние по умолчанию и отдельного скачивания каждого PDF.
- [x] 1.2 Добавить независимые негативные сценарии отсутствия общей кнопки `Скачать отчёт`, version mismatch, отсутствующего, повреждённого, неопубликованного и временно недоступного PDF, а также неавторизованного запроса без публичного Drive URL и ложного success.
- [x] 1.3 Запустить новые сценарии на текущей реализации, зафиксировать ожидаемый RED и подтвердить, что падения вызваны отсутствующим поведением, а не тестовой средой.

## 2. Контракт последней успешной пары

- [x] 2.1 Добавить прикладное представление последней успешной версии, которое атомарно связывает краткую сводку, рекомендацию, `Итоги` и `ABC-тест` с одним кандидатом, вакансией, профилем и версией анализа.
- [x] 2.2 Разрешать представление пары только после успешной проверки REP-009 и не переводить кандидата в `READY`, если один документ отсутствует, повреждён, не опубликован либо не согласован со вторым.
- [x] 2.3 При подтверждении повторного запуска немедленно скрывать прежнюю пару, сводку и рекомендацию; добавить focused tests отсутствия HR history и fallback при processing/FAILED.

## 3. Защищённая выдача PDF

- [x] 3.1 Добавить прикладные операции preview и download конкретного типа документа с проверкой аутентификации, разрешения, кандидата, вакансии, типа и версии до выдачи содержимого.
- [x] 3.2 Обеспечить выдачу сохранённого неизменяемого PDF без создания или раскрытия прямого публичного URL Google Drive и без передачи клиенту сервисных учётных данных.
- [x] 3.3 Реализовать отдельное скачивание открытого `Итоги` либо `ABC-тест` с нормативным именем REP-005 и аудитом экспорта по SEC-005.
- [x] 3.4 Добавить focused security и contract tests для отказа неавторизованному запросу, version mismatch, неверному типу документа и недоступному файлу без fallback и ложного success.

## 4. Карточка, материалы и modal preview

- [x] 4.1 Сохранить исходные материалы и добавить в раздел `Материалы` отдельную группу `Результаты` ровно с двумя кнопками `Итоги` и `ABC-тест` последней успешной версии.
- [x] 4.2 Удалить из карточки общую кнопку `Скачать отчёт` и не добавлять иной общий либо объединённый download flow.
- [x] 4.3 Реализовать read-only modal для точного PDF, состояния загрузки и понятной ошибки; действие скачивания разместить только внутри modal выбранного документа.
- [x] 4.4 Возвращать ту же карточку в обычное состояние по умолчанию без отдельного сохранения вкладки, scroll, раскрытых блоков, history или focus и без навигации в Google Drive или на другой экран.
- [x] 4.5 Поддержать чтение/прокрутку, масштабирование, поиск, печать и сохранение или скачивание открытого PDF без изменения содержимого; добавить focused UI/accessibility tests.
- [x] 4.6 Сохранить краткую сводку с одной canonical рекомендацией актуальной версии и не показывать result placeholders или disabled-кнопки до публикации пары.

## 5. Ошибки и согласованность

- [x] 5.1 Не показывать кнопки готовой пары и success-сообщения до публикации двух валидных согласованных PDF.
- [x] 5.2 Показывать в modal явную ошибку и только действие закрытия при недоступном, повреждённом или не соответствующем версии PDF, не подставляя другой результат и не предлагая recovery.
- [x] 5.3 Проверить, что preview и download используют один version-bound descriptor и одинаково отклоняют несовпадение идентичности или версии.
- [x] 5.4 Проверить отсутствие отдельного audit-события preview и сохранение существующего аудита экспорта для печати/сохранения/скачивания.

## 6. Проверка и release gate

- [x] 6.1 Повторно запустить независимо созданные acceptance-сценарии и focused contract/security/UI tests и зафиксировать GREEN по REP-015, REP-016, TST-077 и TST-078.
- [x] 6.2 Запустить `npm test` и `npm run lint` из `web/`, устранить внесённые change регрессии и отдельно отметить ранее существующие ошибки, если они не относятся к реализации.
- [ ] 6.3 Запустить `E2E-RESULT-001` с проверкой новой UI-ветки и обязательный полный набор `E2E-VAC-001`, `E2E-TRN-001`, `E2E-ABC-001`, `E2E-RESULT-001` на одной сборке и конфигурации; при отсутствии сценария или доказательств оставить change незавершённым.
- [x] 6.4 Проверить evidence package: две успешные версии, два modal preview, два отдельных скачивания, негативные ответы, отсутствие публичных Drive URL и общей кнопки скачивания, синтетические данные, идентификаторы сборки и независимость автора тестов.
