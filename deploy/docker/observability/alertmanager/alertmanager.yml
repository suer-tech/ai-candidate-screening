global:
  resolve_timeout: 5m

route:
  receiver: retained-for-pulse
  group_by: [system, alertname, severity]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: retained-for-pulse
