# VPS/PostgreSQL runtime — independent acceptance GREEN

Generated: 2026-08-21T06:47:14.045Z

Scope: synthetic acceptance only. The private `candidate/**` folder was not read; no real PII, credentials, provider calls, or provider expense were used.

Normative note: this is change-scoped acceptance for a proposed delta. Conflicting main-spec Cloudflare D1/R2 and corporate Shared Drive/service-account statements remain normative until the explicit post-GREEN sync task; this baseline changes neither.

Result: **11/11 GREEN**, 0 product-contract failures, 0 import/fixture/harness errors.

| Scenario | Contract | Status | Classification | Safe evidence |
|---|---|---:|---|---|
| ATDD-VPSPG-001 | Forbid Cloudflare Workers, D1, R2, Miniflare, Wrangler, and Sites from the production graph and readiness. | GREEN | accepted | POSTGRES_READINESS_CONTRACT_MISSING |
| ATDD-VPSPG-002 | Exercise clean and supported-upgrade migration contracts without production data. | GREEN | accepted | none |
| ATDD-VPSPG-003 | Prove single claim, stale-worker fencing, reconciliation, atomic outbox, and bounded immutable blobs. | GREEN | accepted | none |
| ATDD-VPSPG-004 | Require a Node target, authenticated route semantics, and background-only long work without Cloudflare bindings. | GREEN | accepted | NODE_NITRO_RUNTIME_NOT_READY |
| ATDD-VPSPG-005 | Require one env file and one exact credential directory; reject inline, unknown, escaped, corporate Google, and Cloudflare settings. | GREEN | accepted | UNIFIED_CONFIGURATION_NOT_READY |
| ATDD-VPSPG-006 | Prove consent-first role isolation, deny-checksum audit, offline hard oracle, and unconditional cleanup using synthetic fixtures only. | GREEN | accepted | PRIVATE_BENCHMARK_RUNTIME_NOT_IMPLEMENTED |
| ATDD-VPSPG-007 | Render the same server-provided 55 percent milestone in accessible dashboard and list progress bars. | GREEN | accepted | PROGRESS_UI_CONTRACT_MISSING |
| ATDD-VPSPG-008 | Require the benchmark to use only the explicitly approved immutable profile snapshot and fail closed on missing approval, checksum mismatch, or implicit regeneration before reading pipeline inputs or calling providers. | GREEN | accepted | none |
| ATDD-VPSPG-009 | Prove reference ABC/result and extracted anchors never create or mutate the profile and never reach provider payload, Drive snapshot, or blob store; the profile fingerprint stays independent of reference content. | GREEN | accepted | none |
| ATDD-VPSPG-010 | Require exactly two owner-only generated PDFs retained privately until review/deadline, proven deletion with aggregated evidence, and terminal RED when any cleanup phase is incomplete. | GREEN | accepted | none |
| ATDD-VPSPG-011 | Require E2E-VAC-001/TRN-001/ABC-001/RESULT-001 through the assembled Node web/worker and durable PostgreSQL on one build/config/fixture identity; direct in-memory pipeline or SQLite fixture state does not close the gate. | GREEN | accepted | none |

## Reproduction

```powershell
cd web
node tests/vps-postgres-runtime.evidence.mjs
node --test --test-reporter=junit --test-reporter-destination=tests/acceptance/evidence/vps-postgres-runtime-green.junit.xml tests/vps-postgres-runtime.acceptance.test.mjs
```

The evidence command intentionally exits non-zero while any acceptance contract is RED. JSON and timeline output are written before exit. JUnit is generated by the Node test runner. A product RED is valid only when `infrastructureErrors` remains zero.

## Safety and cleanup

All fixtures use reserved synthetic identities and fixed synthetic checksums. Evidence is rejected if any credential or PII sentinel is serialized. The rendered UI compiler removes its temporary directory in `finally`; the benchmark contract requires cleanup even when its offline oracle is RED.
